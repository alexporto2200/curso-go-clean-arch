# GraphQL schema

type Order {
  id: ID!
  desc: String!
  createdAt: String!
  updatedAt: String!
}

input NewOrder {
  desc: String!
}

type Query {
  listOrders: [Order!]!
}

type Mutation {
  createOrder(input: NewOrder!): Order!
}